interface:
  display_name: "去 AI 味"
  short_description: "按原语言重写文字，保留事实、逻辑、语体和作者声口"
  default_prompt: "Use $qu-ai-wei to rewrite this text in the same language. Preserve every fact, claim, speaker role, formatting detail, and the writer's voice."
